server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/car_rental
    username: root
    password: 2002Olegnote7
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
